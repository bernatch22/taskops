{"actor": "dev:me", "body": {"criteria": ["WHEN paper_realize corre para un book cuyas posiciones tienen open(D+1) y open(D+2) en el lago THE SYSTEM SHALL escribir la fila de paper_book de ese día SIN sizear ningún día nuevo", "WHEN paper_realize corre dos veces THE SYSTEM SHALL no duplicar ninguna fila de paper_book (first-write-wins)", "WHEN el bar 00:00 de un día falta THE SYSTEM SHALL dejar ese día SIN fechar en vez de sustituir el precio", "WHEN el timer axion-v3-paper-realize está instalado THE SYSTEM SHALL correr a una hora POSTERIOR a la llegada del bar 00:00 (~03:30 UTC)"]}, "id": "1912290180f5482e", "kind": "acceptance", "task": "tk-935930", "ts": 1785485217.0686886}
{"actor": "dev:me", "body": {"criteria": ["WHEN paper_realize corre para un book cuyas posiciones tienen open(D+1) y open(D+2) en el lago THE SYSTEM SHALL escribir la fila de paper_book de ese día SIN sizear ningún día nuevo", "WHEN paper_realize corre dos veces THE SYSTEM SHALL no duplicar ninguna fila de paper_book (first-write-wins)", "WHEN el bar 00:00 de un día falta THE SYSTEM SHALL dejar ese día SIN fechar en vez de sustituir el precio", "WHEN el timer axion-v3-paper-realize está instalado THE SYSTEM SHALL correr a una hora POSTERIOR a la llegada del bar 00:00 (~03:30 UTC)"]}, "id": "0d788d0005f28e5e", "kind": "acceptance", "task": "tk-5199fa", "ts": 1785485241.1132457}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the deploy completes THE SYSTEM SHALL have /home/berna/axion-v3 on the box at main's HEAD with screener.py carrying five ConditionSpec entries and gate_toll importable", "WHEN `axion migrate` runs on the box THE SYSTEM SHALL report no pending migrations", "WHEN the fleet is inspected after deploy THE SYSTEM SHALL show every axion-v3-* unit active with timers scheduled", "WHEN the card closes THE SYSTEM SHALL carry a written root-cause paragraph for the 07-30 divergence in its thread"]}, "id": "16ca9041818ed606", "kind": "acceptance", "task": "tk-235ea9", "ts": 1785829294.857733}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the nightly regime_days recompute runs THE SYSTEM SHALL maintain the etf_flow dim forward without manual intervention", "WHEN a day has no ETF flow print THE SYSTEM SHALL store NULL for that day, never 0 and never a forward-fill", "WHEN the card closes THE SYSTEM SHALL show max(day) of non-null etf_flow in prod later than 2026-07-30"]}, "id": "92c3eac992e9d8b9", "kind": "acceptance", "task": "tk-fabaee", "ts": 1785829294.8582826}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the script is committed THE SYSTEM SHALL carry the pre-declaration (subject, both layers, comparison, decision rule) in a commit that precedes any result", "WHEN the re-screen runs THE SYSTEM SHALL recompute the rank under the wide membership AND build the panel without active_universe(), with delisted symbols present until their delisting and NULL after", "WHEN the result is reported THE SYSTEM SHALL compare against #106's original numbers and state which side of the pre-declared decision rule it lands on", "WHEN the card closes THE SYSTEM SHALL have written nothing to trials, forward_track or feature_values, and docs/25 and docs/28 SHALL carry the numbers"]}, "id": "2b7ea8a7e4605df0", "kind": "acceptance", "task": "tk-4e6f7e", "ts": 1785829294.8586798}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the re-materialisation runs THE SYSTEM SHALL pass the membership table explicitly and never rely on pit_membership()'s default", "WHEN the chosen population differs from the stored history THE SYSTEM SHALL materialise under new feature names rather than silently replacing rows", "WHEN the card closes THE SYSTEM SHALL show both features covering 2021-08-01 → present at ~20 members/day under the chosen rule, and tk-3e24c6 SHALL be unblocked in time for its 2026-09-25 window"]}, "id": "908b55c58bf21dbf", "kind": "acceptance", "task": "tk-21a340", "ts": 1785829294.8590758}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the two generations are diffed THE SYSTEM SHALL name the exact cards and commits that disappeared", "WHEN the root cause is found THE SYSTEM SHALL demonstrate it with a reproduction, not a hypothesis", "WHEN the fix lands THE SYSTEM SHALL regenerate 2026-07-30 reporting no less than the max_seq=450 version knew, and the corrected dossier SHALL be in the trunk"]}, "id": "d502fa0d4d9e9ca4", "kind": "acceptance", "task": "tk-34f854", "ts": 1785829294.8594074}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the runbook is rewritten THE SYSTEM SHALL contain no lxc/LXD instruction unless a container verifiably exists", "WHEN an operational command appears in docs/21 or CLAUDE.md §4 THE SYSTEM SHALL have been executed on the box during this card with its output confirming it works", "WHEN the card closes THE SYSTEM SHALL record in its thread when and why the container was removed, or an explicit statement that it could not be reconstructed"]}, "id": "4720e17b31b07ed8", "kind": "acceptance", "task": "tk-da01fa", "ts": 1785829294.8596845}
{"actor": "dev:berna", "body": {"criteria": ["WHEN a symbol that should have bars delivers none for a configurable trailing window THE SYSTEM SHALL emit a completeness event on the bus naming the table, symbols and missing hours", "WHEN a symbol is past its delisting date THE SYSTEM SHALL NOT count its absence as a hole", "WHEN the 2026-07-23 outage shape is replayed against the check THE SYSTEM SHALL fire", "WHEN the card closes THE SYSTEM SHALL have the timer unit live on the box and CLAUDE.md §7 no longer declaring the alert as missing"]}, "id": "1312d0f4adb253ae", "kind": "acceptance", "task": "tk-dc52ed", "ts": 1785829294.86003}
{"actor": "dev:berna", "body": {"criteria": ["WHEN a deploy is attempted from a checkout with uncommitted changes under a synced path THE SYSTEM SHALL refuse, or sync only committed content, rather than shipping the working tree silently", "WHEN the chosen guard is implemented THE SYSTEM SHALL be verified against the 2026-08-04 shape: an uncommitted file under a synced path must not reach the remote", "WHEN the card closes THE SYSTEM SHALL record on the card which guard shape Berna chose and why the others were rejected", "WHEN the worker workflow is decided THE SYSTEM SHALL state whether per-card worktrees are required for cards that deploy, and where that is written down"]}, "id": "12444a2870e1c528", "kind": "acceptance", "task": "tk-5c4602", "ts": 1785831031.7066827}
{"actor": "dev:berna", "body": {"criteria": ["WHEN a universe row carries a NULL listing_date THE SYSTEM SHALL classify that symbol as unknown_window and SHALL NOT raise", "WHEN a NULL listing_date is present THE SYSTEM SHALL still evaluate every other symbol in the same scan", "WHEN the guard lands THE SYSTEM SHALL be pinned by a test that fails without it"]}, "id": "ecb4546e3dd0b154", "kind": "acceptance", "task": "tk-fbb7d2", "ts": 1785831298.0147274}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the docs are corrected THE SYSTEM SHALL state that the three axion VMs and their disks are deleted and that no GCP rollback exists, replacing the reversible-rollback claim rather than rewording it", "WHEN the firewall parenthetical is addressed THE SYSTEM SHALL either name the verified reason the rule cannot be created or carry no explanation at all, given billingEnabled is true", "WHEN the public entry point is documented THE SYSTEM SHALL name the host that actually serves bernardocastro.dev and /api/axion/ with the command that proves it, and SHALL NOT reference a bc-web instance that does not exist", "WHEN any operational command survives in docs/21 or CLAUDE.md THE SYSTEM SHALL have executed it during this card with its output confirming it", "WHEN the card closes THE SYSTEM SHALL have left CLAUDE.md §7 untouched"]}, "id": "c489370bcd6ebfc4", "kind": "acceptance", "task": "tk-765a3e", "ts": 1785831422.4342284}
{"actor": "dev:berna", "body": {"criteria": ["WHEN a card's status changes between backlog and ready THE SYSTEM SHALL record an event for that transition", "WHEN every status mutation is audited THE SYSTEM SHALL show which ones previously recorded no event and that each now does", "WHEN a dossier renders in_flight, blocked or waiting for a past day THE SYSTEM SHALL derive them from events inside that window rather than from current status", "WHEN transitions predate the fix THE SYSTEM SHALL document that those sections cannot be reconstructed for those days, rather than presenting current status as history"]}, "id": "50034152a69f7612", "kind": "acceptance", "task": "tk-3a1473", "ts": 1785832562.3742964}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the regime runner reports coverage THE SYSTEM SHALL report every column of regime_days, derived columns included, from a single shared source rather than an enumerated list", "WHEN a column receives no values in a run THE SYSTEM SHALL report it explicitly rather than omitting the line", "WHEN a column is present in the table but absent from the writer's output THE SYSTEM SHALL be shown by a test to report it, verified by stubbing that column's attach step", "WHEN the card closes THE SYSTEM SHALL state whether any column other than etf_flow was eroded over 2026-07-01 → 08-01"]}, "id": "a64ea13f9b752eff", "kind": "acceptance", "task": "tk-8cbff0", "ts": 1785832562.374887}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the backup target is inspected THE SYSTEM SHALL state whether it resides on the same physical disk and the same machine as the live database", "WHEN a restore is exercised THE SYSTEM SHALL restore the newest backup into a scratch database without touching the live one, and report row counts for forward_track, trials and event_log against live", "WHEN container-level backup is checked THE SYSTEM SHALL report whether any lxc snapshot or export of the axion container exists and its date", "WHEN the card closes THE SYSTEM SHALL carry a written recommendation for off-machine retention, distinguishing the cost of copying only the immutable evidence tables from copying everything, with the decision left to Berna"]}, "id": "9c01e97bd339cce9", "kind": "acceptance", "task": "tk-9f4bfc", "ts": 1785832600.556833}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the grid drift is quantified THE SYSTEM SHALL report, per published trial, how many symbols its panel grid has lost since the trial ran, using an independent point-in-time source if universe cannot answer for past dates", "WHEN the panel-grid fix is proposed THE SYSTEM SHALL evaluate reusing the existing listing_windows() reader rather than introducing a second definition of membership", "WHEN the provenance gap is addressed THE SYSTEM SHALL propose how a trial records the code version that produced it, after checking whether trials already has a field for it", "WHEN the card closes THE SYSTEM SHALL have written nothing to trials, forward_track or feature_values, and SHALL NOT adopt or recommend adopting a yardstick change as decided"]}, "id": "21501bcfafd8704b", "kind": "acceptance", "task": "tk-790332", "ts": 1785832869.3436894}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the inventory is taken THE SYSTEM SHALL record on the card what sat under the box's .taskops/ with sizes before anything is removed", "WHEN deletion proceeds THE SYSTEM SHALL have first demonstrated that no deployed code or unit references .taskops on the box", "WHEN the card closes THE SYSTEM SHALL show the box free of .taskops/trees, db.sqlite* and remote.json, with the local machine's .taskops untouched", "WHEN the credential copy is assessed THE SYSTEM SHALL state its unix permissions on the box and leave the rotate-or-not decision to Berna with the facts written down", "WHEN shipway.yml is checked THE SYSTEM SHALL confirm the excludes cover .taskops/remote.json as well, adding the exclude if missing"]}, "id": "5ddbfcb129900ea6", "kind": "acceptance", "task": "tk-ebacbd", "ts": 1785833686.8348987}
{"actor": "dev:berna", "body": {"criteria": ["WHEN a claimed card is released on one store and the event replays on another THE SYSTEM SHALL show the same (empty) assignee on both", "WHEN the fix lands THE SYSTEM SHALL be pinned by a test that fails without it", "WHEN the audit completes THE SYSTEM SHALL name every event kind whose local mutation is richer than its replayed projection, with each either fixed or argued safe", "WHEN pre-fix stale assignees are considered THE SYSTEM SHALL state whether they self-heal or need repair, with the reasoning on the card"]}, "id": "392e1f16628804bc", "kind": "acceptance", "task": "tk-6cb5e9", "ts": 1785835782.3708317}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the commit lands THE SYSTEM SHALL contain the five named dossiers in git and touch no other path", "WHEN a dossier is malformed THE SYSTEM SHALL name it on the card and commit the sound ones rather than all-or-nothing", "WHEN the card closes THE SYSTEM SHALL leave the 2026-08-04 server/local report conflict UNRESOLVED and recorded, never force-pushed", "WHEN the card closes THE SYSTEM SHALL state whether .mcp.json belongs in .gitignore without having committed or ignored it"]}, "id": "7c5b5f0edc9c7a10", "kind": "acceptance", "task": "tk-9079a3", "ts": 1785939387.771044}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the audit is written THE SYSTEM SHALL list every authorized_keys entry on the box with its comment, type and fingerprint, for every user that can log in", "WHEN an entry names an owner THE SYSTEM SHALL state whether that owner still exists, verified against the current topology rather than against the key comment", "WHEN a removal is proposed THE SYSTEM SHALL give the per-entry reason and leave the removal itself unexecuted", "WHEN the card closes THE SYSTEM SHALL state whether any automation still authenticates through the entries it proposes removing"]}, "id": "96a4acfcc536009c", "kind": "acceptance", "task": "tk-1f99d2", "ts": 1785940014.5632277}
{"actor": "dev:berna", "body": {"criteria": ["WHEN each of the five files is corrected THE SYSTEM SHALL state the VMs were DELETED with their disks and that recovery is a rebuild, never an instances start", "WHEN docs/30 is corrected THE SYSTEM SHALL preserve what was believed at that date rather than rewriting the entry into hindsight", "WHEN deploy/axion-v3-api.service is edited THE SYSTEM SHALL change only the comment and leave the 0.0.0.0 bind untouched", "WHEN the card closes THE SYSTEM SHALL confirm docs/21, CLAUDE.md, shipway.yml and these five files agree, and name any disagreement found", "WHEN the sweep is finished THE SYSTEM SHALL show that no file outside docs/22 still claims the VMs were stopped"]}, "id": "a54968bda31bb841", "kind": "acceptance", "task": "tk-87c9d2", "ts": 1785940114.2353482}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the audit is written THE SYSTEM SHALL name every file carrying 10.128.0.12 and state whether the gpubox.conf drop-in overrides each one", "WHEN the address is assessed THE SYSTEM SHALL confirm from allocation state, not from an instance listing, whether 10.128.0.12 is free to be claimed", "WHEN a fix is proposed THE SYSTEM SHALL say what must be redeployed and whether anything silently falls back to the hardcoded default", "WHEN the card closes THE SYSTEM SHALL have changed no unit on the box and restarted nothing"]}, "id": "ed0b84fb1f736ea6", "kind": "acceptance", "task": "tk-174b80", "ts": 1785940114.2357988}
{"actor": "dev:berna", "body": {"criteria": ["WHEN the defect is reported THE SYSTEM SHALL show the test greening from the shared checkout against a branch file that should have failed it", "WHEN the fix lands THE SYSTEM SHALL make the test read the tree it lives in, so the same run from either directory tests the same file", "WHEN the sweep for siblings finishes THE SYSTEM SHALL name every other test that resolves a repo-root path rather than a fixture", "WHEN docs/29 is updated THE SYSTEM SHALL record the trap with its date and the run that caught it", "WHEN the card closes THE SYSTEM SHALL have weakened no existing assertion"]}, "id": "525f646f50842801", "kind": "acceptance", "task": "tk-de2771", "ts": 1785940346.0525236}
