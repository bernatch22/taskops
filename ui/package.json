{
  "name": "taskops-ui",
  "private": true,
  "version": "0.1.0",
  "description": "The taskops dashboard: React over the board's own HTTP contracts.",
  "type": "module",
  "scripts": {
    "typecheck": "node smoke/sections.mjs && tsc --noEmit",
    "build": "npm run typecheck && node build.mjs",
    "smoke": "node smoke/run.mjs",
    "check": "npm run build && npm run smoke && git diff --exit-code ../src/taskops/ui"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
