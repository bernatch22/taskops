{
  "name": "taskops-ui",
  "private": true,
  "version": "0.1.0",
  "description": "The taskops web interface: a live board over the engine's own HTTP contracts.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && node build.mjs",
    "check": "npm run build && git diff --exit-code ../src/taskops/transports/http/ui"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
