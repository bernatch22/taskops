<!-- taskops:report date=2026-07-28 max_seq=55 generated=2026-07-28T12:32:41 -->

# 2026-07-28 — 8 closed · 0 in flight · 0 blocked · 10 commit(s) · 6 actor(s)

## Cerrado (8)

✓ **tk-dc436a** — Rename studio -> taskops ui (command, dir, docs)
  agent:berna/v21 · held 8m · 2 commit(s) · +146 -75
  `276bbbef6145` rename: taskops studio is now taskops ui, and studio/ is ui/ (+143 -72)
    .gitignore, ARCHITECTURE.md, CHANGELOG.md, README.md +45 more
  `2442773e2690` ui: the read-only and token refusals name the command that prints the URL (+3 -3)
    src/taskops/transports/http/policy.py
  1 comment(s) · last: Renamed taskops studio -> taskops ui. (1) git mv studio/ -> ui/ and commands/studio.py -> ui.py; the subparser register…

✓ **tk-a21435** — report day: the deterministic daily dossier (engine + render + CLI +…
  agent:berna/v22 · held 10m · 1 commit(s) · +793 -48
  `931a00ac73ab` report day: one calendar day in full, with each commit's diff size (+793 -48)
    CHANGELOG.md, README.md, USAGE.md, docs/flujos.html +23 more
  1 comment(s) · last: report day shipped: contracts/day.py (DayReport/ClosedCard/CommitStat), engine/day.py (local-midnight window via mktime…

✓ **tk-66b0c9** — Regroup the CLI: taskops tasks domain, hook group hidden, agent group
  agent:berna/v21 · held 6m · 1 commit(s)
  `69a69e9` cli: a tasks domain for people, and the plumbing steps out of --help (+0 -0)
    CHANGELOG.md, USAGE.md, src/taskops/render/__init__.py, src/taskops/render/_text.py +6 more
  1 comment(s) · last: Regrouped the CLI: new `taskops tasks` domain (list/show/add/plan/done/release/log/search), each subcommand a wrapper o…

✓ **tk-9bcbb0** — report day --write + digest: reporte del dia persistido en .taskops/r…
  agent:berna/v21 · held 7m · 2 commit(s) · +5 -1
  `288ee1a` report day --write: the dossier filed under .taskops/reports/, fingerprinted, plus /taskops:digest (+0 -0)
    CHANGELOG.md, USAGE.md, plugin/skills/digest/SKILL.md, src/taskops/__init__.py +16 more
  `9d69017788a4` report: the day helper takes the Path the caller already had (+5 -1)
    .taskops/events.jsonl, src/taskops/transports/cli/commands/report.py
  1 comment(s) · last: report day --write persists .taskops/reports/YYYY-MM-DD.md with a max_seq fingerprint and an empty ## Narracion section…

✓ **tk-74c6fb** — taskops run: dispatch --spawn renombrado como experimento, con la adv…
  agent:berna/v22 · held 4m · 1 commit(s)
  `38b46b6` taskops run: the spawn path under a name that says what it costs (+0 -0)
    CHANGELOG.md, src/taskops/transports/cli/commands/dispatch.py, src/taskops/transports/cli/commands/run_.py, src/taskops/transports/cli/main.py +3 more
  1 comment(s) · last: taskops run added as a visible command (= usecases.dispatch spawn=True) with the billing warning, confirmation, --yes f…

✓ **tk-1726d3** — tasks edit: editar spec/title/priority de una card (evento edited + r…
  agent:berna/v21 · held 6m · 1 commit(s)
  `7dabd1a` tasks edit: a card's title, spec and priority can be corrected (+0 -0)
    CHANGELOG.md, USAGE.md, src/taskops/_types.py, src/taskops/contracts/__init__.py +11 more
  1 comment(s) · last: tasks edit shipped. New `edited` event kind (one event per changed field), TaskTable.set_field guarded by an EDITABLE_F…

✓ **tk-588e0a** — taskops tasks lists a finished project instead of saying nothing
  agent:berna/fix2 · held 4m · 1 commit(s) · +306 -30
  `8a9709eaf1c9` tasks: a finished project lists its closed cards instead of nothing (+306 -30)
    src/taskops/render/_tasklist.py, src/taskops/render/tasklist.py, src/taskops/transports/cli/commands/_tasks_args.py, src/taskops/transports/cli/commands/tasks.py +2 more
  1 comment(s) · last: tasks now falls back to the closed cards when nothing is open (heading, newest-updated first, capped at 10 with +N more…

✓ **tk-85e55b** — A worker must never spend the API key — strip ANTHROPIC_* from the sp…
  agent:berna/fix1 · held 5m · 1 commit(s) · +151 -20
  `35291bbaf31e` A worker must never spend the API key (+151 -20)
    CHANGELOG.md, USAGE.md, src/taskops/assets/GUIDE.md, src/taskops/engine/_process.py +5 more
  1 comment(s) · last: Workers no longer inherit the Anthropic credentials. engine/worker.DROPPED_ENV (ANTHROPIC_API_KEY/AUTH_TOKEN/BASE_URL)…

## Conversaciones (8)

**agent:berna/v21** on tk-dc436a: Renamed taskops studio -> taskops ui. (1) git mv studio/ -> ui/ and commands/studio.py -> ui.py; the subparser registers as `ui` with help "serve the live web…

**agent:berna/v22** on tk-a21435: report day shipped: contracts/day.py (DayReport/ClosedCard/CommitStat), engine/day.py (local-midnight window via mktime, so a DST day is 23 or 25h and never st…

**agent:berna/v21** on tk-66b0c9: Regrouped the CLI: new `taskops tasks` domain (list/show/add/plan/done/release/log/search), each subcommand a wrapper over an existing run — tasks show/search…

**agent:berna/v21** on tk-9bcbb0: report day --write persists .taskops/reports/YYYY-MM-DD.md with a max_seq fingerprint and an empty ## Narracion section; refuses overwrite unless --force (new…

**agent:berna/v22** on tk-74c6fb: taskops run added as a visible command (= usecases.dispatch spawn=True) with the billing warning, confirmation, --yes for unattended callers, free --dry-run, E…

**agent:berna/v21** on tk-1726d3: tasks edit shipped. New `edited` event kind (one event per changed field), TaskTable.set_field guarded by an EDITABLE_FIELDS whitelist (SQL stays in storage/),…

**agent:berna/fix2** on tk-588e0a: tasks now falls back to the closed cards when nothing is open (heading, newest-updated first, capped at 10 with +N more), plus --all and --status <s> (refused…

**agent:berna/fix1** on tk-85e55b: Workers no longer inherit the Anthropic credentials. engine/worker.DROPPED_ENV (ANTHROPIC_API_KEY/AUTH_TOKEN/BASE_URL) + a drop param on _process.spawn that PO…

## Por actor

| actor | tasks | commits | comments | closed |
|---|---|---|---|---|
| dev:berna | 8 | 0 | 0 | 0 |
| dev:me | 5 | 6 | 0 | 0 |
| agent:berna/v21 | 4 | 3 | 4 | 4 |
| agent:berna/v22 | 2 | 1 | 2 | 2 |
| agent:berna/fix1 | 1 | 0 | 1 | 1 |
| agent:berna/fix2 | 1 | 0 | 1 | 1 |

## Narración

## Lo que necesita un humano

Nada quedó bloqueado ni reclamado en vuelo: el encabezado del dossier declara `8 closed · 0 in flight · 0 blocked`, y las ocho cards cerradas tienen al menos un commit asociado. No hay ninguna card cerrada sin entrega visible.

Hay, en cambio, cuatro problemas de contabilidad en el propio dossier que conviene mirar antes de confiar en sus números:

- **Cuatro commits figuran con diff `+0 -0` aunque tocan entre 7 y 20 archivos:** `69a69e9` (10 archivos), `288ee1a` (20 archivos), `38b46b6` (7 archivos) y `7dabd1a` (15 archivos). Como consecuencia, tres cards —tk-66b0c9, tk-74c6fb y tk-1726d3— no tienen ningún total de diff en su línea de resumen, y el total de tk-9bcbb0 (`+5 -1`) sólo cuenta su segundo commit. El titular del día, «10 commit(s)», es fiable; el volumen de cambio del día no lo es.
- **La tabla por actor no cuadra con la autoría de los commits listados bajo las cards.** `dev:me` aparece con 6 commits y 0 cards cerradas; `agent:berna/fix1` y `agent:berna/fix2` aparecen con 0 commits cada uno, pese a que cada una de sus cards (tk-85e55b, +151 −20, y tk-588e0a, +306 −30) lleva un commit propio. La suma de commits de la tabla sí da 10, así que no falta nada: lo que no coincide es a quién se le atribuye cada cosa.
- **`dev:berna` figura con 8 tareas, 0 commits, 0 comentarios y 0 cierres.** Es decir: hay dos identidades humanas en el registro (`dev:berna` y `dev:me`) y la que abrió las tareas no es la que aparece firmando commits.
- **El commit `9d69017788a4` incluye `.taskops/events.jsonl`**, el propio log de eventos del que se genera este reporte, dentro de un commit del día que el reporte describe.

Una limitación más, que afecta a todo lo que sigue: **el dossier no trae el bloque `**Pedido**` de ninguna card**. Sólo trae el título, y varios títulos están truncados (`…`). Lo mismo pasa con los ocho comentarios: todos están cortados a mitad de frase. Por eso, más abajo, «lo que se pidió» se lee del título y nada más, y hay compromisos que probablemente estén escritos después del corte y que aquí no aparecen.

## Por área

### UI web y transporte HTTP

**tk-dc436a — Rename studio -> taskops ui (command, dir, docs).** Lo pedido, según el título, era un rename en tres planos: el comando, el directorio y la documentación. Lo entregado fueron dos commits. El primero, `276bbbef6145` («rename: taskops studio is now taskops ui, and studio/ is ui/», +143 −72), tocó 49 archivos: `.gitignore`, `ARCHITECTURE.md`, `CHANGELOG.md`, `README.md` y 45 más. El comentario detalla el mecanismo: «(1) git mv studio/ -> ui/ and commands/studio.py -> ui.py; the subparser registers as `ui` with help "serve the live web…» —el resto del comentario está truncado en el dossier—. El segundo commit, `2442773e2690` («ui: the read-only and token refusals name the command that prints the URL», +3 −3), tocó un solo archivo, `src/taskops/transports/http/policy.py`: el rename no se detuvo en el subparser ni en los docs, sino que alcanzó los mensajes de rechazo de la política HTTP, que ahora nombran el comando que imprime la URL. Ese segundo commit es un cambio que el título de la card no pedía explícitamente. Costo: 8 minutos retenida, 2 commits, +146 −75.

### CLI: superficie de comandos

**tk-66b0c9 — Regroup the CLI: taskops tasks domain, hook group hidden, agent group.** El título pide tres cosas: un dominio `tasks`, ocultar el grupo de hooks y algo relativo a un «agent group». Se entregó un commit, `69a69e9` («cli: a tasks domain for people, and the plumbing steps out of --help»), sobre 10 archivos: `CHANGELOG.md`, `USAGE.md`, `src/taskops/render/__init__.py`, `src/taskops/render/_text.py` y 6 más. El comentario documenta el primer punto con precisión —«new `taskops tasks` domain (list/show/add/plan/done/release/log/search), each subcommand a wrapper over an existing run — tasks show/search…»— y el asunto del commit («the plumbing steps out of --help») cubre el segundo. **Del «agent group» del título no hay rastro en la parte visible del dossier**: ni el asunto del commit ni el tramo legible del comentario lo mencionan, y el comentario está cortado justo en «tasks show/search…». No se puede afirmar ni que se hizo ni que no se hizo. La decisión de diseño que sí queda registrada es que cada subcomando de `tasks` es un envoltorio sobre un run que ya existía, no una implementación nueva. Costo: 6 minutos retenida, 1 commit, sin diff registrado (el commit figura como +0 −0 sobre 10 archivos).

**tk-74c6fb — taskops run: dispatch --spawn renombrado como experimento, con la adv… (título truncado).** El pedido, tal como lo enuncia el título, era renombrar el camino `dispatch --spawn` «como experimento» con una advertencia. Lo entregado fue `38b46b6` («taskops run: the spawn path under a name that says what it costs») sobre 7 archivos: `CHANGELOG.md`, `src/taskops/transports/cli/commands/dispatch.py`, `src/taskops/transports/cli/commands/run_.py`, `src/taskops/transports/cli/main.py` y 3 más. El comentario describe algo más ancho que un rename: «taskops run added as a visible command (= usecases.dispatch spawn=True) with the billing warning, confirmation, --yes for unattended callers, free --dry-run, E…» —truncado ahí—. **Hay una divergencia entre el pedido y la entrega en el encuadre**: el título habla de un rename marcado como experimento, y el comentario habla de un comando *visible* añadido, con confirmación interactiva, escape `--yes` para llamadores desatendidos y un `--dry-run` gratuito. La palabra «experimento» no aparece en el asunto del commit ni en el tramo legible del comentario. La decisión de fondo es explícita en el asunto: exponer el camino de spawn bajo un nombre que dice lo que cuesta. Costo: 4 minutos retenida, 1 commit, sin diff registrado (+0 −0 sobre 7 archivos).

**tk-1726d3 — tasks edit: editar spec/title/priority de una card (evento edited + r… (título truncado)).** El pedido: poder editar spec, título y prioridad de una card, con un evento `edited` y algo más que el título corta. Se entregó `7dabd1a` («tasks edit: a card's title, spec and priority can be corrected») sobre 15 archivos, incluidos `CHANGELOG.md`, `USAGE.md`, `src/taskops/_types.py` y `src/taskops/contracts/__init__.py`, más 11 archivos adicionales. El comentario fija dos decisiones: el nuevo tipo de evento emite **un evento por campo cambiado**, no uno por edición —«New `edited` event kind (one event per changed field)»—, y `TaskTable.set_field` queda acotado por una whitelist (`EDITABLE_F…` en el corte del comentario; la sección `## Narración` del propio dossier la nombra como `EDITABLE_FIELDS` y añade que «el SQL se queda en `storage/`»). Los tres campos del título aparecen los tres en el asunto del commit, así que el pedido está cubierto. Costo: 6 minutos retenida, 1 commit, sin diff registrado (+0 −0 sobre 15 archivos).

### Reportes: engine, contratos, render y persistencia

**tk-a21435 — report day: the deterministic daily dossier (engine + render + CLI +… (título truncado)).** Lo pedido era un dossier diario determinista que cruzara engine, render y CLI —el cuarto término del título está cortado—. Es la entrega más grande del día: un solo commit, `931a00ac73ab` («report day: one calendar day in full, with each commit's diff size»), +793 −48 sobre 27 archivos, entre ellos `CHANGELOG.md`, `README.md`, `USAGE.md` y `docs/flujos.html` más 23 archivos adicionales. El comentario nombra las piezas nuevas: «contracts/day.py (DayReport/ClosedCard/CommitStat), engine/day.py (local-midnight window via mktime, so a DST day is 23 or 25h and never st…» —truncado—. **La decisión que un lector no adivinaría por el título es la de la ventana temporal**: medianoche local calculada con `mktime`, con la consecuencia asumida de que un día con cambio de horario dura 23 o 25 horas. El determinismo prometido en el título se paga en que el día no es de duración fija. El asunto del commit añade un requisito que el título no menciona: cada commit lleva su tamaño de diff. Costo: 10 minutos retenida, 1 commit, +793 −48.

**tk-9bcbb0 — report day --write + digest: reporte del dia persistido en .taskops/r… (título truncado).** El pedido: `--write` sobre `report day` más un digest, con el reporte persistido bajo `.taskops/reports/`. Dos commits. `288ee1a` («report day --write: the dossier filed under .taskops/reports/, fingerprinted, plus /taskops:digest») cubrió 20 archivos —`CHANGELOG.md`, `USAGE.md`, `plugin/skills/digest/SKILL.md`, `src/taskops/__init__.py` y 16 más—, es decir que el digest se entregó como skill del plugin. El comentario fija el contrato del archivo: «persists .taskops/reports/YYYY-MM-DD.md with a max_seq fingerprint and an empty ## Narracion section…» —truncado ahí—. Dos elementos merecen subrayarse: el archivo lleva una **huella `max_seq`** que lo ata al log de eventos del que salió, y **nace con la sección `## Narración` vacía**, a llenar por el skill. El segundo commit, `9d69017788a4` («report: the day helper takes the Path the caller already had», +5 −1), corrige la firma del helper de día en `src/taskops/transports/cli/commands/report.py` para que reciba el `Path` que el llamador ya tenía —un ajuste de API sobre código publicado minutos antes en la misma card— e incluye además `.taskops/events.jsonl`. Costo: 7 minutos retenida, 2 commits; el total registrado (+5 −1) corresponde sólo al segundo, porque el primero figura como +0 −0.

### Render de listados

**tk-588e0a — taskops tasks lists a finished project instead of saying nothing.** El pedido está entero en el título: que un proyecto terminado liste algo en vez de no decir nada. Se entregó `8a9709eaf1c9` («tasks: a finished project lists its closed cards instead of nothing», +306 −30) sobre 6 archivos: `src/taskops/render/_tasklist.py`, `src/taskops/render/tasklist.py`, `src/taskops/transports/cli/commands/_tasks_args.py`, `src/taskops/transports/cli/commands/tasks.py` y 2 más. El comentario describe el comportamiento exacto: «tasks now falls back to the closed cards when nothing is open (heading, newest-updated first, capped at 10 with +N more), plus --all and --status <s> (refused…» —truncado justo donde empezaba a explicar un rechazo—. **Los flags `--all` y `--status <s>` no aparecen en el título**: son alcance añadido sobre lo pedido. Y queda una regla a medias en el registro: el paréntesis «(refused…» indica que `--status` rechaza algún valor, pero el dossier corta antes de decir cuál. Costo: 4 minutos retenida, 1 commit, +306 −30.

### Engine: proceso y workers

**tk-85e55b — A worker must never spend the API key — strip ANTHROPIC_* from the sp… (título truncado).** El pedido: que un worker no pueda gastar la API key, quitando las variables `ANTHROPIC_*` del spawn. Se entregó `35291bbaf31e` («A worker must never spend the API key», +151 −20) sobre 9 archivos: `CHANGELOG.md`, `USAGE.md`, `src/taskops/assets/GUIDE.md`, `src/taskops/engine/_process.py` y 5 más. El comentario da la forma de la solución y la separación de responsabilidades: «engine/worker.DROPPED_ENV (ANTHROPIC_API_KEY/AUTH_TOKEN/BASE_URL) + a drop param on _process.spawn that PO…» —truncado—. Es decir: **la lista de variables prohibidas vive en `worker` y el mecanismo de descarte es un parámetro `drop` en `_process.spawn`**, no un filtrado incrustado en el lanzador. Las tres variables cubiertas son `ANTHROPIC_API_KEY`, `ANTHROPIC_AUTH_TOKEN` y `ANTHROPIC_BASE_URL`, lo cual satisface el `ANTHROPIC_*` del título. Que se tocara también `src/taskops/assets/GUIDE.md` indica que el cambio se documentó para los propios agentes, cosa que el título no pedía. Costo: 5 minutos retenida, 1 commit, +151 −20.

## Decisiones y sorpresas

**La ventana del día no dura 24 horas a propósito.** `engine/day.py` calcula la medianoche local con `mktime`, y el comentario de tk-a21435 asume la consecuencia sin disculparse: un día con cambio de horario tiene 23 o 25 horas «and never st…». Es la decisión menos adivinable del día a partir de los títulos: se eligió que el día calendario local sea la unidad, aun a costa de que su duración varíe.

**El reporte escrito nace incompleto por diseño.** `report day --write` deja en disco un `.taskops/reports/YYYY-MM-DD.md` con una sección `## Narración` vacía y una huella `max_seq`. El archivo no pretende ser el producto final: es el soporte que el skill `/taskops:digest` —añadido en el mismo commit, en `plugin/skills/digest/SKILL.md`— viene a completar. La huella `max_seq` ata cada archivo al punto del log de eventos del que salió.

**El camino caro se expone en vez de esconderse.** En tk-74c6fb, `dispatch --spawn` no quedó detrás de una bandera: se convirtió en `taskops run`, comando visible, «under a name that says what it costs», con advertencia de facturación y confirmación, un `--yes` para llamadores desatendidos y un `--dry-run` gratuito. El título de la card lo llamaba «experimento»; la entrega lo trata como comando de primera clase con fricción deliberada.

**El nuevo dominio `tasks` no reimplementa nada.** Cada subcomando (`list/show/add/plan/done/release/log/search`) es «a wrapper over an existing run». En la misma card, el plumbing sale de `--help`: hay una separación explícita entre lo que se le muestra a una persona y lo que existe para la máquina.

**El log de ediciones es por campo, no por edición.** `tasks edit` emite un evento `edited` por cada campo cambiado. La escritura pasa por `TaskTable.set_field` detrás de una whitelist de campos editables, y —según la narración previa del propio dossier— el SQL se mantiene en `storage/`: la regla de capas se reafirmó en lugar de saltarse por conveniencia.

**Las credenciales se cortan en el proceso, no en la política.** La lista `DROPPED_ENV` vive en `engine/worker`, pero el descarte lo ejecuta un parámetro `drop` en `_process.spawn`. Política arriba, mecanismo abajo.

**Sorpresa de alcance en el rename.** El paso de `studio` a `ui` no fue sólo `git mv` y docs: tocó 49 archivos y necesitó un segundo commit para que los rechazos de read-only y de token en `transports/http/policy.py` nombraran el comando correcto. Un rename de comando arrastró texto de mensajes de error en otra capa.

**Corrección dentro del mismo día.** `9d69017788a4` reajusta el helper de `report day` para que «take the Path the caller already had», minutos después de que el helper se publicara en `288ee1a`. Es el único cambio del día que revierte una decisión de firma tomada en la misma card.

**Desproporción de tamaño.** Con el diff registrado a mano, el día se reparte así: `report day` (+793 −48, 27 archivos), el listado de proyectos terminados (+306 −30, 6 archivos), el corte de credenciales (+151 −20, 9 archivos) y el rename a `ui` (+146 −75, 50 archivos entre sus dos commits). Los otros cuatro commits, que tocan entre 7 y 20 archivos cada uno, no tienen diff registrado.

## Lo que queda abierto

No hay cards sin cerrar: el día terminó con 0 en vuelo y 0 bloqueadas, y ninguna card quedó reclamada.

Lo que sí queda pendiente son deudas de registro y de contenido:

- **Las narraciones de los reportes persistidos.** Por construcción, todo `.taskops/reports/YYYY-MM-DD.md` escrito con `--write` sale con `## Narración` vacía. Cada día que se escriba y no se pase por `/taskops:digest` deja un archivo a medio hacer.
- **El «agent group» de tk-66b0c9.** El título de la card lo pide, y ni el asunto del commit ni la parte visible del comentario dicen qué se hizo con él. Es la única parte de un pedido del día que no se puede dar por entregada con lo que hay en el dossier.
- **La regla de rechazo de `--status` en tk-588e0a.** El comentario se corta en «plus --all and --status <s> (refused…»: hay un caso rechazado cuyo criterio no está documentado en el registro.
- **Los ocho comentarios están truncados.** Si alguno de los agentes anotó un pendiente, una limitación conocida o un seguimiento después del corte, no es visible aquí. Ninguna de las partes legibles nombra un follow-up explícito, pero eso no equivale a que no exista.
- **La contabilidad de diffs y de autoría del propio dossier**, descrita en la primera sección: cuatro commits con `+0 -0` sobre archivos reales, tres cards sin total de diff, dos agentes con cierres pero sin commits atribuidos, y dos identidades humanas (`dev:berna`, `dev:me`) que entre ambas no cierran ninguna card.
